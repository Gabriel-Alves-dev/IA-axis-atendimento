# Dados persistentes do WAHA local (sessões do WhatsApp, mídias) — só na raiz;
# sem a âncora "/" a regra também ignorava app/src/app/api/webhooks/waha/
/waha/

# Dados persistentes de um teste antigo com n8n (não usado mais no projeto)
/n8n/

# Segredos usados pelo docker-compose.yml (WAHA_API_KEY, senha do dashboard etc.)
# e pelo docker-compose.prod.yml na VM (.env.production). O *.example é versionado.
.env
.env.production
